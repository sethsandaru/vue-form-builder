# Control Configuration View of Vue-Form-Builder

Each control has some specific configuration field(s). So to render the configuration fields, we need to create a Vue component for that control here.
